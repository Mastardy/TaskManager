db = db.getSiblingDB("${MONGO_DB}");

db.createUser({
   user: "${MONGO_USER}",
   pwd: "${MONGO_PASSWORD}",
   roles: [
       {
           role: "dbOwner",
           db: "${MONGO_DB}"
       }
   ]
});

db.createCollection("${MONGO_COLLECTION}");
db.${MONGO_COLLECTION}.insertOne({
    title: "Test",
    status: "pending"
})